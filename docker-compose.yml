version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      -"3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    volumes:
      - .:app
      - /app/node_modules